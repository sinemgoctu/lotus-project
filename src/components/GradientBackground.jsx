import React from "react";

const GradientBackground = () => {
  const styles = {
    height: "100vh",
    backgroundImage: "linear-gradient(#FFFFFF, rgb(255, 122, 89))",
    margin: 0,
  };

  return <div style={styles}></div>;
};

export default GradientBackground;
